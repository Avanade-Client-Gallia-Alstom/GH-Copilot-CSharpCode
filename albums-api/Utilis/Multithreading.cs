using System;
using System.IO;
using System.Threading;

namespace albums_api.Utilis
{
    //"Generate a multithreaded C# program using coding standards in #file:'CodingStandard.json' for each method creadted to demonstrate usecase -  where two threads attempt to access the same resource (a file), utilize locking to ensure thread safety."

}
